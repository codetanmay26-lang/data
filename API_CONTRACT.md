# Aadhaar Pulse – API Contract

## GET /aggregate/national
Returns aggregated Aadhaar metrics for India.

## GET /aggregate/state
Returns state-wise aggregated metrics.

## GET /aggregate/district?state=<State Name>
Returns district-wise aggregated metrics for a state.

### Response Format
- JSON
- Numeric values only
- No personal data
